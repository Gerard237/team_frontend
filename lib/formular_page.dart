import 'package:flutter/material.dart';

class formularpage extends StatelessWidget {
  const formularpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
